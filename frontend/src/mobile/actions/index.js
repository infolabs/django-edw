import {
  getEntities,
  readEntities,
  notifyLoadingEntities,
  toggleDropdown,
  selectDropdown,
  setDataViewComponents,
  setCurrentView,
  getEntity,
  hideVisibleDetail
} from './EntityActions';

import {
  loadTree,
  reloadTree,
  readTree,
  notifyLoadingTerms,
  toggleTerm,
  resetTerm,
  resetBranch,
  setPrevTaggedItems
} from './TermsTreeActions';

export default {
  getEntities,
  readEntities,
  notifyLoadingEntities,
  toggleDropdown,
  selectDropdown,
  loadTree,
  reloadTree,
  readTree,
  notifyLoadingTerms,
  toggleTerm,
  resetTerm,
  resetBranch,
  setDataViewComponents,
  setCurrentView,
  getEntity,
  hideVisibleDetail,
  setPrevTaggedItems
}
